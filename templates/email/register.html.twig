{% extends 'email/layer.html.twig' %}

{% block body %}
    {{ content }}
    <div style="margin-top: 1em">
        <a href="{{ absolute_url(path(route, {token: token})) }}" style="
  background-color: rgb(131,129,144) ;
  border-radius: 0.5rem;
  border: 1px solid rgb(131,129,144);
  color: rgb(251,222,193);
  padding: 0.5rem 1rem;
  box-shadow: 0 0.5rem 0.5rem rgba(0, 0, 0, 0.15);">Envoyer</a>
    </div>
{% endblock %}