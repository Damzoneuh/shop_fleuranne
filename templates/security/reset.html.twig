{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Réinitialisation de mot de passe{% endblock %}
{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12 col-md-6 text-pink offset-md-3">
                <div class="p-sm-5 p-2 rounded shadow bg-grey-inherit mt-sm-5 mb-sm-5 mt-2 mb-2">
                    <h1 class="text-center font-weight-bold">Réinitialisation de mot de passe</h1>
                    {{ form_start(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}